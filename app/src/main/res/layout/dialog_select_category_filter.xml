<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#ffffffff"
    android:layout_margin="20dp">

    <TextView
        android:id="@+id/title_select_category_dialog_textview"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:gravity="center"
        android:background="@color/colorGrey"
        android:scaleType="fitCenter"
        android:text="Select Category"
        android:textSize="20dp"
        android:textStyle="bold"
        android:textColor="#000000"/>

    <Button
        android:layout_marginTop="10dp"
        android:id="@+id/select_category_all_btn_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/title_select_category_dialog_textview"
        android:layout_centerInParent="true"
        android:background="@color/buttonCommonColorLight"
        android:text="All"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/select_category_dialog_recyclerview"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/select_category_all_btn_dialog"/>
</RelativeLayout>